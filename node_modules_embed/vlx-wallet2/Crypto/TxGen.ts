import Transaction from "./Transaction";

export default class {
	private readonly sodium;

	constructor(sodium) {
		this.sodium = sodium;
	}

	public generateTx() {
		return new Transaction(this.sodium);
	}

	// public generate = (unspents: Out<BigInteger>[], amount: BigInteger, velasKey: HDKeys, changeAddress: string, to: string, commission: BigInteger) => {
	// 	return new Transaction(this.sodium, unspents, amount, velasKey, changeAddress, to, commission);
	// }
}
