// Generated by LiveScript 1.6.0
(function(){
  var eth, rst, insight, ripple, erc20, omni, stellar, tron, extendProviders, toString$ = {}.toString;
  eth = require('./providers/eth.ls');
  rst = require('./providers/rst.ls');
  insight = require('./providers/insight.ls');
  ripple = require('./providers/ripple.ls');
  erc20 = require('./providers/erc20.ls');
  omni = require('./providers/omni.ls');
  stellar = require('./providers/stellar.ls');
  tron = require('./providers/tron.ls');
  extendProviders = function(providers, config){
    if (toString$.call(config.providers).slice(8, -1) !== 'Object') {
      return;
    }
    return importAll$(providers, config.providers);
  };
  module.exports = function(config, cb){
    var def;
    def = {
      eth: eth,
      insight: insight,
      ripple: ripple,
      erc20: erc20,
      omni: omni,
      stellar: stellar,
      tron: tron,
      rst: rst
    };
    extendProviders(def, config);
    return cb(null, def);
  };
  function importAll$(obj, src){
    for (var key in src) obj[key] = src[key];
    return obj;
  }
}).call(this);
